# Whisper models (large binary files)

Download with:

```bash
npm run download-model
```

Default file: `ggml-base-q5_1.bin` (~57 MB, quantized `base` model).

These files are **not** committed to git. Each user downloads once.
